<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= asset_path 'banner.jpg' %>);">
  <div class="container">
    <h1>Ombu Hub</h1>
    <h2>Ayudando a pequenas personas, lograr cosas gigantes!</h2>
    <p>Aprende haciendo, en equipo y jugando</p>
    <a class="btn btn-flat" href="/courses">Ver Cursos</a>
  </div>
</div>

<div class= "container mt-5">

<%= simple_form_for :search, url: root_path, method: :get do |f| %>
  <div class="search-form-control form-group">
    <input class="form-control string required" type="text" name="search[query]" id="search_query" />
    <button name="button" type="submit" class="btn btn-flat">
      <i class="fas fa-search"></i> Buscar Cursos
    </button>
  </div>
<% end %>

</div>
