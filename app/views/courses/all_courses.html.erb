   <% if user_signed_in?  %>
    <% if current_user.is_admin? %>


      <div class="container">
        <h2 class= "mt-3" >Todos los cursos PRIVADO:</h2>
        <%= render "top" %>
        <div class="row">
                    <%= render 'course_card' %>

            </div>
      </div>


    <% end %>
  <% end %>
